#ifndef ENVPARSER_H
#define ENVPARSER_H

#include <string>

// Declare the function to load the .env file and set the environment variables
void loadEnvFile(const std::string& envFilePath);

#endif // ENVPARSER_H
